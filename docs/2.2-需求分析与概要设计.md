# 2.2 需求分析与概要设计

## 一、需求分析

### 1.1 用户角色

| 角色 | 描述 | 权限范围 |
|------|------|----------|
| 访客 (Guest) | 未登录用户 | 浏览公开照片、搜索、查看时间轴/故事线/年度回顾/分享卡片 |
| 注册用户 (User) | 已登录用户 | 访客权限 + 上传/编辑/删除自己的照片、创建相册、收藏、创建分享卡片、配置 AI |
| 管理员 (Admin) | 拥有管理权限的用户 | 用户权限 + 管理所有照片、用户管理、系统设置 |

### 1.2 功能需求

| 编号 | 功能模块 | 功能描述 | 优先级 | 角色 |
|------|----------|----------|--------|------|
| F1 | 用户认证 | 用户注册、登录、获取当前用户信息、修改密码 | 高 | 全部 |
| F2 | 照片管理 | 上传（单张/批量）、编辑、删除照片 | 高 | 用户/管理员 |
| F3 | 照片浏览 | 瀑布流展示、搜索、按标签/年份/月份筛选 | 高 | 全部 |
| F4 | AI 元数据 | 自动生成照片标题、心情描述、标签建议 | 中 | 用户/管理员 |
| F5 | AI 搜索增强 | 输入时智能推荐搜索关键词和标签 | 中 | 全部 |
| F6 | 标签系统 | 创建/管理标签、为照片添加标签、按标签筛选 | 高 | 全部（创建需登录） |
| F7 | 相册管理 | 创建/编辑/删除相册、添加/移除照片、设置封面 | 中 | 用户/管理员 |
| F8 | 收藏功能 | 收藏/取消收藏照片、查看收藏列表 | 中 | 全部（添加需登录或指纹） |
| F9 | 时间轴 | 按年份/月份分组浏览照片 | 低 | 全部 |
| F10 | 故事线 | 按年月+地点聚合照片展示，AI 生成叙事摘要 | 低 | 全部 |
| F11 | 年度回顾 | 年度统计数据展示，AI 生成年度叙事 | 低 | 全部 |
| F12 | 分享卡片 | 生成可分享的照片卡片（四种模板），AI 生成文案 | 低 | 用户/管理员 |
| F13 | 用户系统 | 个人主页、资料编辑、用户管理（管理员） | 中 | 全部 |
| F14 | AI 设置 | AI 服务提供商/模型/API Key 配置，多预设管理 | 中 | 管理员 |
| F15 | 暗房模式 | 私密照片空间，提供四种沉浸式浏览模式 | 低 | 用户/管理员 |

### 1.3 非功能需求

| 编号 | 需求类型 | 需求描述 |
|------|----------|----------|
| NF1 | 性能 | 图片上传后应在 3 秒内完成压缩处理 |
| NF2 | 性能 | 照片列表 API 响应时间不超过 500ms（100 条以内） |
| NF3 | 安全 | 密码使用 bcrypt 加密存储 |
| NF4 | 安全 | JWT 令牌认证，令牌有效期 7 天 |
| NF5 | 安全 | 用户只能操作自己的照片，管理员可操作全部 |
| NF6 | 可用性 | 前端响应式设计，适配桌面端和移动端 |
| NF7 | 可用性 | 支持暗色模式切换 |
| NF8 | 可扩展 | AI 服务支持多 provider 切换（OpenAI/Ollama/智谱） |
| NF9 | 可扩展 | 图片存储支持本地和 Cloudflare R2 |

### 1.4 用例图

```plantuml
@startuml
left to right direction

actor "访客 (Guest)" as guest
actor "注册用户 (User)" as user
actor "管理员 (Admin)" as admin
actor "AI 系统" as ai

rectangle "光影手记 (Shimmer)" {
  usecase "浏览公开照片" as UC1
  usecase "搜索照片" as UC2
  usecase "查看时间轴" as UC3
  usecase "查看故事线" as UC4
  usecase "查看年度回顾" as UC5
  usecase "查看分享卡片" as UC6
  usecase "用户注册" as UC7
  usecase "用户登录" as UC8
  usecase "上传照片" as UC9
  usecase "编辑/删除照片" as UC10
  usecase "管理标签" as UC11
  usecase "创建相册" as UC12
  usecase "收藏照片" as UC13
  usecase "创建分享卡片" as UC14
  usecase "管理用户" as UC15
  usecase "管理全部照片" as UC16
  usecase "配置 AI 设置" as UC17
  usecase "生成照片元数据" as UC18
  usecase "搜索建议增强" as UC19
  usecase "生成年度回顾叙事" as UC20
  usecase "生成故事线摘要" as UC21
  usecase "生成分享文案" as UC22
}

guest --> UC1
guest --> UC2
guest --> UC3
guest --> UC4
guest --> UC5
guest --> UC6
guest --> UC7
guest --> UC8

user --> UC8
user --> UC9
user --> UC10
user --> UC11
user --> UC12
user --> UC13
user --> UC14

admin --> UC15
admin --> UC16
admin --> UC17

ai --> UC18
ai --> UC19
ai --> UC20
ai --> UC21
ai --> UC22

user ..|> guest : extends
admin ..|> user : extends
@enduml
```

### 1.5 业务活动图（照片上传与 AI 补全流程）

```plantuml
@startuml
title 照片上传与 AI 补全业务活动图

start
:用户选择照片;
:上传到服务器;
if (上传成功?) then (是)
  :显示照片预览;
  :用户填写基本信息\n(标题/心情/地点);
  if (使用 AI 补全?) then (是)
    :请求 AI 生成元数据;
    if (AI 返回成功?) then (是)
      :自动填充标题/心情/标签;
      :用户调整 AI 建议;
    else (失败)
      :手动填写;
    endif
  else (否)
    :手动填写;
  endif
  :添加/确认标签;
  :保存照片;
  if (创建相册?) then (是)
    :添加到相册;
  endif
  :发布照片;
else (失败)
  :显示错误信息;
endif
stop
@enduml
```

## 二、概要设计

### 2.1 系统架构

```plantuml
@startuml
skinparam packageStyle rectangle

package "表现层 (Presentation Layer)" {
  [Vue 3 SPA]
  [Vite 构建工具]
  [Pinia 状态管理]
  [Vue Router 路由]
  [Axios HTTP 客户端]
}

package "API 层 (API Layer)" {
  [Express.js REST API]
  [JWT 认证中间件]
  [CORS 跨域]
  [全局错误处理]
}

package "服务层 (Service Layer)" {
  [用户服务 Auth]
  [照片服务 Photos]
  [标签服务 Tags]
  [相册服务 Albums]
  [收藏服务 Favorites]
  [AI 服务 AI]
  [年度回顾 Review]
  [故事线 Storyline]
  [分享卡片 Share]
  [统计服务 Stats]
}

package "数据层 (Data Layer)" {
  database "MySQL" {
    [users]
    [photos]
    [tags]
    [photo_tags]
    [favorites]
    [albums]
    [album_photos]
    [ai_settings]
    [share_cards]
    [review_cache]
    [story_summaries]
  }
  [Cloudflare R2\n图片对象存储]
}

package "外部服务 (External)" {
  [OpenAI API]
  [Ollama 本地模型]
  [智谱 AI]
}

[Vue 3 SPA] --> [Express.js REST API] : HTTP/JSON
[Express.js REST API] --> [用户服务 Auth]
[Express.js REST API] --> [照片服务 Photos]
[Express.js REST API] --> [AI 服务 AI]
[照片服务 Photos] --> [Cloudflare R2]
[AI 服务 AI] --> [OpenAI API]
[AI 服务 AI] --> [Ollama 本地模型]
[AI 服务 AI] --> [智谱 AI]
... --> [MySQL] : SQL 查询

@enduml
```

### 2.2 ER 图（数据库设计）

```plantuml
@startuml

!define primary_key <&key>
!define foreign_key <&pin>

' 实体定义
entity users {
  * id: BIGINT UNSIGNED [PK]
  --
  username: VARCHAR(50) [UNIQUE, NOT NULL]
  password_hash: VARCHAR(255) [NOT NULL]
  nickname: VARCHAR(50)
  avatar: VARCHAR(500)
  role: ENUM('user','admin')
  bio: TEXT
  created_at: DATETIME
  updated_at: DATETIME
}

entity photos {
  * id: BIGINT UNSIGNED [PK]
  --
  user_id: BIGINT UNSIGNED [FK, NOT NULL]
  title: VARCHAR(200)
  url: VARCHAR(500) [NOT NULL]
  thumbnail_url: VARCHAR(500)
  original_url: VARCHAR(500)
  mood: VARCHAR(100)
  shot_date: DATE
  location: VARCHAR(200)
  camera: VARCHAR(100)
  lens: VARCHAR(100)
  aperture: VARCHAR(20)
  shutter_speed: VARCHAR(20)
  iso: INT
  width: INT
  height: INT
  file_size: INT
  sort_order: INT
  visibility: ENUM('public','private','hidden')
  latitude: DECIMAL(10,7)
  longitude: DECIMAL(10,7)
  created_at: DATETIME
  updated_at: DATETIME
}

entity tags {
  * id: BIGINT UNSIGNED [PK]
  --
  name: VARCHAR(50) [UNIQUE, NOT NULL]
  color: VARCHAR(7)
  created_at: DATETIME
}

entity photo_tags {
  * photo_id: BIGINT UNSIGNED [FK]
  * tag_id: BIGINT UNSIGNED [FK]
}

entity favorites {
  * id: BIGINT UNSIGNED [PK]
  --
  photo_id: BIGINT UNSIGNED [FK, NOT NULL]
  user_id: BIGINT UNSIGNED [FK]
  fingerprint: VARCHAR(64)
  created_at: DATETIME
}

entity albums {
  * id: BIGINT UNSIGNED [PK]
  --
  user_id: BIGINT UNSIGNED [FK, NOT NULL]
  title: VARCHAR(100) [NOT NULL]
  description: TEXT
  cover_photo_id: BIGINT UNSIGNED [FK]
  is_public: BOOLEAN
  sort_order: INT
  created_at: DATETIME
  updated_at: DATETIME
}

entity album_photos {
  * album_id: BIGINT UNSIGNED [FK]
  * photo_id: BIGINT UNSIGNED [FK]
  --
  added_at: DATETIME
  sort_order: INT
}

entity ai_settings {
  * id: BIGINT UNSIGNED [PK]
  --
  name: VARCHAR(100)
  is_active: TINYINT(1)
  provider: VARCHAR(50)
  model: VARCHAR(100)
  base_url: VARCHAR(500)
  api_key: VARCHAR(500)
  timeout: INT
  enabled: TINYINT(1)
  created_at: DATETIME
  updated_at: DATETIME
}

entity share_cards {
  * id: INT [PK]
  --
  share_id: VARCHAR(24) [UNIQUE, NOT NULL]
  user_id: INT
  photo_ids: JSON [NOT NULL]
  template: VARCHAR(32)
  custom_text: TEXT
  ai_caption: TEXT
  story_date: DATE
  story_location: VARCHAR(255)
  view_count: INT
  is_deleted: TINYINT(1)
  created_at: DATETIME
  updated_at: DATETIME
  deleted_at: DATETIME
}

entity review_cache {
  * id: BIGINT UNSIGNED [PK]
  --
  year: INT [UNIQUE, NOT NULL]
  ai_summary: TEXT
  model: VARCHAR(100)
  provider: VARCHAR(50)
  generated_at: DATETIME
  updated_at: DATETIME
}

entity story_summaries {
  * id: BIGINT UNSIGNED [PK]
  --
  story_date: VARCHAR(10) [UNIQUE, NOT NULL]
  summary_text: TEXT [NOT NULL]
  photo_hash: VARCHAR(64)
  created_at: DATETIME
  updated_at: DATETIME
}

' 关系定义
users ||--o{ photos : "拥有"
users ||--o{ albums : "创建"
users ||--o{ favorites : "收藏"
users ||--o{ ai_settings : "配置"

photos ||--o{ photo_tags : "标注"
photos ||--o{ favorites : "被收藏"
photos ||--o{ album_photos : "包含于"
photos ||--o{ albums : "封面" : cover_photo_id

tags ||--o{ photo_tags : "使用"

albums ||--o{ album_photos : "包含"

@enduml
```

### 2.3 功能模块分解

```plantuml
@startuml
package "光影手记 (Shimmer)" {
  package "用户模块" {
    [用户注册]
    [用户登录]
    [JWT 认证]
    [个人主页]
    [用户管理(管理员)]
  }

  package "照片模块" {
    [照片上传(单张/批量)]
    [照片编辑/删除]
    [照片列表浏览]
    [照片搜索筛选]
    [EXIF 数据提取]
    [图片压缩处理]
  }

  package "AI 智能模块" {
    [元数据生成]
    [搜索增强]
    [年度回顾叙事]
    [故事线摘要]
    [分享文案生成]
    [AI 配置管理]
  }

  package "标签模块" {
    [标签 CRUD]
    [照片-标签关联]
    [AI 建议标签]
    [热门标签]
  }

  package "社交模块" {
    [收藏功能]
    [分享卡片]
    [时间轴]
    [故事线]
    [年度回顾]
  }

  package "相册模块" {
    [相册 CRUD]
    [照片管理]
    [封面设置]
  }

  package "暗房模块" {
    [胶片暗房]
    [显影液模式]
    [聚光灯模式]
    [光绘模式]
  }
}

@enduml
```

### 2.4 数据库表清单

| 表名 | 引擎 | 说明 | 核心字段 |
|------|------|------|----------|
| `users` | InnoDB | 用户表 | id, username, password_hash, nickname, avatar, role, bio |
| `photos` | InnoDB | 照片表 | id, user_id, title, url, thumbnail_url, mood, shot_date, location, visibility, camera, lens, aperture, shutter_speed, iso, latitude, longitude |
| `tags` | InnoDB | 标签表 | id, name (UNIQUE), color |
| `photo_tags` | InnoDB | 照片-标签关联表 | photo_id (FK), tag_id (FK) 复合主键 |
| `favorites` | InnoDB | 收藏表 | id, photo_id (FK), user_id (FK, 可空), fingerprint (访客标识) |
| `albums` | InnoDB | 相册表 | id, user_id (FK), title, description, cover_photo_id (FK), is_public |
| `album_photos` | InnoDB | 相册-照片关联表 | album_id (FK), photo_id (FK) 复合主键, added_at |
| `ai_settings` | InnoDB | AI 配置表 | id, name, is_active, provider, model, base_url, api_key, timeout, enabled |
| `share_cards` | InnoDB | 分享卡片表 | id, share_id (UNIQUE), user_id, photo_ids (JSON), template, custom_text, ai_caption |
| `review_cache` | InnoDB | 年度回顾缓存 | id, year (UNIQUE), ai_summary, model, provider |
| `story_summaries` | InnoDB | 故事线摘要缓存 | id, story_date (UNIQUE), summary_text, photo_hash |
